{% layout 'layout.liquid' %}
{% block content %}
<div class="container-sm">
    <form action="/feed" method="POST">
        <fieldset>
            <legend>Create New Example</legend>
            <label class="form-label">Title: </label>
            <input 
                type="text" 
                name="title" 
                placeholder="enter example title"
                class="form-control"
            >
            <label class="form-label">Body:</label>
            <input 
                type="text" 
                name="body" 
                placeholder="enter example body" 
                class="form-control"
            >
            <label class="form-label">Amount:</label>
            <input 
                type="number" {% layout 'layout.liquid' %}
{% block content %}
<div class="container-md">
    <article>
        <h2>
            {{ feed.title }} -
            {% if feed.ready %}
            Ready
            {% else %}
            Not Ready
            {%endif%}
        </h2>
        <h3>Amount: {{ feed.amount }}</h3>
        <h6>Body: {{ feed.body }}</h6>
        {% if feed.owner == userId %}
        <div class="btn-group">

            <a href="/feeds/{{ feed.id }}/edit" class="btn btn-primary">Edit</a>
            <form action="/feeds/{{ feed.id }}?_method=DELETE" method="POST">
                <input type="submit" value="Delete" class="btn btn-danger">
            </form>
        </div>
        {% endif %}
    </article>
</div>
{% endblock %}
                name="amount" 
                class="form-control"
            >
            <label class="form-label">Ready:</label>
            <input 
                type="checkbox" 
                name="ready" 
                class="form-check-input"
            >
        </fieldset>
        <input type="submit" value="Create New Example" class="btn btn-success">
    </form>
</div>
{% endblock %}